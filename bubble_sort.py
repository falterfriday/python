# itereate through the array of values
	# taking the first two, sorting them, then the next two


def list(arr):
	for i in range(0,len(arr)):
		# print arr[i]
		for j in range(0,len(arr) -i):
			# print arr[j]
			if 


list([3,2,1,0])